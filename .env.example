# ModelLogue Environment Variables

# --- Docker Compose ---
TIMEZONE=Asia/Tokyo

# --- Frontend (Vite) ---
VITE_PLANTUML_SERVER_URL=http://localhost:8080
VITE_N8N_BASE_URL=http://localhost:5678

# --- n8n ---
# Gemini API key is configured inside n8n UI (Credentials),
# NOT as an environment variable. This keeps secrets out of
# files and follows the "no secrets in code" principle.
